<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.edux">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/edux_log"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/edux_log"
        android:supportsRtl="true"
        android:theme="@style/Theme.EduX">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainPageActivity">

        </activity>
        <activity android:name=".OtherActivities.ExaminationOpenActivity">

        </activity>
        <activity android:name=".OtherActivities.NotesOpenActivity">

        </activity>

        <activity android:name=".OtherActivities.ArticleOpenActivity">

        </activity>

        <activity android:name=".OtherActivities.PersonalNotesActivity">

        </activity>

    </application>

</manifest>